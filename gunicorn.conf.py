"""
إعدادات Gunicorn — يقرأها تلقائياً أي أمر `gunicorn wsgi:app` (Gunicorn
يحمّل ./gunicorn.conf.py تلقائياً من مجلد التشغيل، بدون أي حاجة لتعديل
Start Command بـRender).

المشكلة اللي هذا الملف يحلها:
القيمة الافتراضية لـ--timeout عند Gunicorn هي 30 ثانية فقط. الـworker
المتزامن (sync، الافتراضي) ما يبعث أي "نبضة حياة" للـmaster process
طول ما هو مشغول برد واحد — حتى لو كان يبث SSE فعلياً ويرسل بيانات
للمستخدم أولاً بأول. يعني أي رد ياخذ أطول من 30 ثانية إجمالاً (مشروع
كامل متعدد الملفات، رد بوضع "مفكر"/"مبرمج" يستخدم أدوات مدمجة كالبحث
أو تنفيذ الكود، أو موديل 120b بـreasoning عالي) يخلي Gunicorn يقتل
الـworker بمنتصف الرد قبل ما يخلص — يوصل المستخدم اتصال منقطع فجأة
يظهر كـ"خطأ في الخادم" بلا أي سبب واضح. وهذا بالضبط يفسّر ليش المشكلة
تكبر مع حجم/تعقيد الطلب: كل ما طال وقت التوليد المتوقع، زاد احتمال
تجاوز الـ30 ثانية.
"""
timeout = 300  # ثانية — بدل الافتراضي 30، كافية لأطول رد متوقع (مشروع كامل)
graceful_timeout = 30
